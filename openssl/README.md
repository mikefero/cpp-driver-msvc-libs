# OpenSSL

Each archive contains the associated OpenSSL version with all the header files,
shared and static libraries required for building the DataStax C/C++ driver.
Each archive also contains the OpenSSL command line executable (shared and
statically linked).

+---include
¦   +---openssl
+---shared
¦   +---bin
¦   +---lib
+---static
    +---bin
    +---lib
